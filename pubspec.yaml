name: litigation_management_app
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter

    #tauqeer bhai in sb ko last mai upgrade krna abhi nahi

  cupertino_icons: ^1.0.7
  flutter_riverpod: ^2.6.1        # downgrade
  intl: ^0.19.0                   # ⬇downgrade
  pdf: ^3.11.1
  printing: ^5.13.2
  file_picker: ^8.1.0             #  downgrade
  google_fonts: ^6.2.1            # downgrade
  url_launcher: ^6.3.1
  path_provider: ^2.1.4
  shared_preferences: ^2.3.4     # downgrade
  font_awesome_flutter: ^10.7.0   # downgrade
  uuid: ^4.4.2
  flutter_local_notifications: ^19.0.0  # downgrade
  timezone: ^0.10.1                     # downgrade
  firebase_core: ^3.13.0          # downgrade
  firebase_auth: ^5.5.0           # downgrade
  cloud_firestore: ^5.6.0         # downgrade
  table_calendar: ^3.1.2
  get_it: ^7.7.0                  # downgrade

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700

